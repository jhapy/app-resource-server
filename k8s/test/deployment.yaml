apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-resource-server
  labels:
    tier: backend
  namespace: thothee-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-resource-server
      tier: backend
  template:
    metadata:
      labels:
        app: app-resource-server
        tier: backend
    spec:
      containers:
        - name: app-resource-server
          image: jhapy/app-resource-server:v2
          env:
            - name: JAVA_OPTS
              value: '-Xms256m -Xmx512m -Dspring.cloud.config.label=test -Dspring.cloud.config.uri=http://admin:admin@utils-registry-server.thothee-test.svc.cluster.local:8888/config -Djhapy.registry.password=admin'
            - name: SERVER_NAME
              value: '{{.Node.Hostname}}-{{.Node.ID}}'
            - name: SPRING_PROFILES_ACTIVE
              value: k8s-test
          resources:
            requests:
              memory: '256Mi'
              cpu: '250m'
            limits:
              memory: '512Mi'
              cpu: '1'
          ports:
            - name: http
              containerPort: 9005
            - name: management
              containerPort: 9105
          readinessProbe:
            httpGet:
              path: /management/health/readiness
              port: management
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /management/health/liveness
              port: management
            initialDelaySeconds: 120