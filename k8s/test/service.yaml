apiVersion: v1
kind: Service
metadata:
  name: app-resource-server
  labels:
    tier: backend
  namespace: thothee-test
spec:
  type: NodePort
  selector:
    app: app-resource-server
  ports:
    - name: http
      port: 9005
      targetPort: 9005
